import styles from "./Mensagem.module.css";

function Mensagem() {
  return (
    <div className={styles.container}>
      <h1 className={styles.titulo}> MEU FORMULÁRIO </h1>
    </div>
  );
}

export default Mensagem;
